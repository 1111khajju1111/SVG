package com.thevault.backend.model;

public enum EnquiryType {
    /** "Ask about customizing this piece" from a product page. */
    PRODUCT,
    /** "Order via WhatsApp" from the cart. */
    ORDER,
    /** The contact form on the home page. */
    CONTACT
}
