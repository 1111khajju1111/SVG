<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.thevault.backend</groupId>
	<artifactId>SSS</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>SSS</name>
	<description>SSS jewelry works</description>

	<properties>
		<!-- Spring Boot 4 requires Java 21 as its minimum baseline (Jakarta EE 11).
		     Your own Eclipse project (.settings/org.eclipse.jdt.core.prefs) is
		     already set to source/target 21 — this was the mismatch. -->
		<java.version>21</java.version>
		<jjwt.version>0.12.6</jjwt.version>
		<aws-sdk.version>2.28.11</aws-sdk.version>
	</properties>

	<dependencies>
		<!-- Boot 4 deprecates spring-boot-starter-web in favor of this -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>

		<!-- MySQL driver (used for Aiven MySQL) -->
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>

		<!-- JWT (manual auth, no Spring Security). jjwt-gson, not jjwt-jackson:
		     jjwt 0.12.6's Jackson provider depends on Jackson 2.x internally,
		     which conflicts with Spring Boot 4's Jackson 3 — this is what was
		     actually broken before, just not loudly. Gson sidesteps it. -->
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>${jjwt.version}</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>${jjwt.version}</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-gson</artifactId>
			<version>${jjwt.version}</version>
			<scope>runtime</scope>
		</dependency>

		<!-- Password hashing (BCrypt) without pulling in Spring Security -->
		<dependency>
			<groupId>org.mindrot</groupId>
			<artifactId>jbcrypt</artifactId>
			<version>0.4</version>
		</dependency>

		<!-- S3-compatible object storage for jewelry images (AWS S3 or
		     Cloudflare R2 via endpoint override). Only active at runtime if
		     app.storage.provider=s3 — see ImageStorage / S3ImageStorage. -->
		<dependency>
			<groupId>software.amazon.awssdk</groupId>
			<artifactId>s3</artifactId>
			<version>${aws-sdk.version}</version>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

</project>
